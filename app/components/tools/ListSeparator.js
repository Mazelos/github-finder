import React from 'react';
import { View } from 'react-native';

const ListSeparator = () => {
  return (
    <View
      style={{
        width: "100%",
        height: 10
      }}
    />
  );
}

export default ListSeparator;